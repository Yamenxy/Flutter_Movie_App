name: movies_app
description: A minimal Flutter project scaffold.
publish_to: 'none'
version: 0.0.1
environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^4.4.0
  google_sign_in: ^6.1.0
  flutter_svg: ^2.0.7
  vector_graphics: ^1.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/
